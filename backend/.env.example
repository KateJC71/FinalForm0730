# Database
DATABASE_URL=./reservation.db

# Server
PORT=3000
NODE_ENV=production

# Frontend URL (for CORS)
FRONTEND_URL=https://reservation.furano-snowforce.com

# Google Sheets Integration
GOOGLE_SHEETS_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Email Configuration (Gmail SMTP)
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
STORE_EMAIL=store@furano-snowforce.com

# Notes:
# 1. SMTP_PASS should be Gmail App Password, not regular password
# 2. Enable 2FA on Gmail and generate App Password: https://support.google.com/accounts/answer/185833
# 3. STORE_EMAIL is where new reservation notifications will be sent